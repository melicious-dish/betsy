<h2>List of Products</h2>

<ul>
  <% @products.each do |p| %>
  <li>
    <strong>
      <%= p.name %>
      <%= p.price.to_f.round(2) %>
    </strong>

    <div>

    </div>
  </li>

  <div><strong>Add to Item to Cart</strong>
    <%=  %>
  </div>
  <% end %>
</ul>
